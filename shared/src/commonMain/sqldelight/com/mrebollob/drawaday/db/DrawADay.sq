
CREATE TABLE DrawImage(
date TEXT NOT NULL PRIMARY KEY,
image TEXT NOT NULL,
source TEXT NOT NULL
);

insertItem:
INSERT OR REPLACE INTO DrawImage(date, image, source)VALUES(?,?, ?);

selectAll:
SELECT * FROM DrawImage;

deleteAll:
DELETE FROM DrawImage;